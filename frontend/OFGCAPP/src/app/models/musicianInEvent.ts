export class MusicianInEvent {
  id: string;
  category: string;
  special: string;
  constructor(id: string, category: string, special: string) {
    this.id = id;
    this.category = category;
    this.special = special;
  }
}
